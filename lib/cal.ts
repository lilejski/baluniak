/**
 * The one booking link. The embed on the home page and the plain link on the
 * recruiter page both read it from here, so a changed event type is a
 * one-line edit rather than a hunt.
 */

export const CAL_ORIGIN = "https://cal.com";
export const CAL_LINK = "łukasz-bałuniak-wafkto/30min";
export const CAL_URL = `${CAL_ORIGIN}/${CAL_LINK}`;
